This folder should contain the image format png, in the title containing the type of hardware.
Name and file extension must be in a small case.
Examples:
  ipcamera.png
  server-windows.png
  router.png
  switch.png

The selected hardware type is stored in host inventory field "type".
For example, inventory type "switch" uses hardware/switch.png.
